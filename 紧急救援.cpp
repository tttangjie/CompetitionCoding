#include <stdio.h> 
#include <string.h>
#include <stdlib.h>
#include <algorithm>
#define N 510

int main() {
	int n, m, from, to;
	int num[N]; 
	int i, a, b, len;
	scanf("%d%d%d%d", n, m, from, to);
	for (i=0; i<n; i++) {
		scanf("%d", &num[i]);
	}
	for (i=0; i<m; i++) {
		scanf("%d%d%d", &a, &b, &line);

	}
	
	return 0;
}
